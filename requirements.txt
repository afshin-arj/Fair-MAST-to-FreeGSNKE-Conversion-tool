# Fresh-machine install (Windows / Linux / macOS)
#
#   python3.11 -m venv .venv
#   # Windows: .venv\Scripts\activate
#   # Unix:    source .venv/bin/activate
#   python -m pip install -U pip
#   python -m pip install -r requirements.txt
#
# Or one-shot:
#   Windows: setup_fresh.cmd
#   Unix:    ./setup_fresh.sh
#
# FreeGSNKE execution uses a separate venv — scripts/ensure_freegsnke_env.py
# s5cmd: scripts/ensure_s5cmd.py
#
-e ".[ui,dev]"
